gg
I need to restructure the existing FBPA AI Agent Run Console architecture.

IMPORTANT:
Do NOT redesign the entire application from scratch.
Do NOT remove or break the existing agents.
Reuse the existing UI components, Airflow fields, validation logic, SharePoint upload logic, and code-generation logic wherever possible.

CURRENT APPLICATION STRUCTURE

The current home/sidebar has the following top-level agents:

1. Home
2. FBPA Developer Code Agent
3. BSA Agent
4. Infrastructure Build Agent
5. Tester Agent
6. Production Support Agent

Inside FBPA Developer Code Agent, there are multiple AWS/service-specific options such as:

- Airflow
- Glue
- Lambda
- etc.

Currently, I have implemented the Airflow functionality inside FBPA Developer Code Agent.

CURRENT AIRFLOW FLOW

When the user selects:

FBPA Developer Code Agent
    → Airflow

the application currently displays an Airflow request form containing fields such as:

- Choose Service
- Define the Task
    - Create new DAG
    - Modify existing DAG
- DAG Pattern
    - Static DAG
    - Dynamic DAG
- Scheduling details
- Tasks to be added
- Task-specific arguments
- Additional information
- Other existing Airflow-specific fields

At the end, there is currently an option to:

Generate Intake Form: Yes / No

If Yes:
- Generate the intake form
- Upload it to SharePoint

If No:
- Generate the Airflow code directly from the entered information.

THIS CURRENT DESIGN MUST CHANGE.

NEW REQUIRED ARCHITECTURE

Add a NEW top-level agent:

Intake Agent

The sidebar/home structure should become:

Home
FBPA Developer Code Agent
Intake Agent
BSA Agent
Infrastructure Build Agent
Tester Agent
Production Support Agent

Do NOT place Intake Agent under FBPA Developer Code Agent.

It must be a separate top-level agent.

--------------------------------------------------
INTAKE AGENT
--------------------------------------------------

The primary responsibility of Intake Agent is to create standardized intake forms.

When the user selects:

Intake Agent
    → Airflow

the user should see the Airflow intake fields.

Move/reuse the existing Airflow intake fields from the current FBPA Developer Code Agent implementation.

For example:

- Choose Service
- Define the Task
- New DAG / Existing DAG
- Static DAG / Dynamic DAG
- Scheduling configuration
- Task configuration
- Task-specific arguments
- Additional information
- Any other fields that currently exist in the Airflow implementation

Do NOT duplicate these fields unnecessarily.

Reuse the existing components/functions where possible.

There should NO LONGER be:

"Generate Intake Form: Yes / No"

inside the Airflow Developer Agent.

The Intake Agent itself is responsible for generating the intake form.

After the user completes the Intake Agent form:

1. Validate the input.
2. Build the intake-form data structure.
3. Generate the intake form using the existing intake-form generation logic.
4. Upload the intake form to SharePoint using the existing SharePoint integration.
5. Display a success message.
6. Provide a clear indication/reference of the generated intake form.
7. Store/return the structured intake data in a format that can be consumed by the Developer Code Agent.

The design should allow us to add future intake types later:

Intake Agent
    → Airflow Intake
    → Glue Intake
    → Lambda Intake
    → Future service intakes

Do not hard-code the Intake Agent architecture only for Airflow.

--------------------------------------------------
FBPA DEVELOPER CODE AGENT
--------------------------------------------------

The existing FBPA Developer Code Agent must remain a separate top-level agent.

When the user selects:

FBPA Developer Code Agent
    → Airflow

the Airflow Developer Agent should now focus ONLY on code generation/modification.

It should NOT recreate the complete intake form.

Instead, the first step of the Airflow Developer Agent should be to provide an option to supply the intake.

For example:

Step 1 - Provide Airflow Intake

Options:

1. Upload Intake Form
2. Use an existing/generated intake if available

The user must be able to upload the intake form generated by Intake Agent.

The user should also be able to upload an intake form from their local machine.

Therefore the Airflow Developer Agent should support:

- Upload intake form from local machine
- Use the intake generated by Intake Agent, when available

--------------------------------------------------
AIRFLOW DEVELOPER FLOW
--------------------------------------------------

The new Airflow Developer Agent flow should conceptually be:

FBPA Developer Code Agent
    ↓
Choose Service
    ↓
Airflow
    ↓
Upload Airflow Intake
    ↓
Parse Intake
    ↓
Validate Intake
    ↓
Generate/Modify Airflow DAG
    ↓
Display/Save Generated Code

The developer agent should extract all required Airflow configuration from the uploaded intake instead of asking the user to manually enter the same information again.

For example, if the intake contains:

Task Type = Create New DAG
DAG Pattern = Dynamic DAG
Schedule = <schedule>
Tasks = <tasks>
Arguments = <arguments>

the Airflow Developer Agent should use those values directly.

--------------------------------------------------
IMPORTANT: DO NOT LOSE EXISTING AIRFLOW LOGIC
--------------------------------------------------

The existing Airflow code-generation functionality is already implemented.

Do NOT rewrite the DAG-generation logic unless necessary.

Do NOT change existing Airflow business rules unnecessarily.

The goal is to separate:

1. Intake collection/form generation
2. Airflow code generation

while preserving the existing functionality.

Refactor the existing code into reusable modules/functions where appropriate.

--------------------------------------------------
DATA FLOW
--------------------------------------------------

The desired overall flow is:

                 ┌─────────────────┐
                 │   Intake Agent  │
                 └────────┬────────┘
                          │
                          ▼
                 Airflow Intake Form
                          │
                  ┌───────┴────────┐
                  │                │
                  ▼                ▼
             SharePoint       Local Upload
                                   │
                                   ▼
                    ┌────────────────────────┐
                    │ FBPA Developer Agent   │
                    │        → Airflow       │
                    └───────────┬────────────┘
                                │
                                ▼
                         Parse Intake
                                │
                                ▼
                       Generate DAG Code

The Intake Agent and Developer Code Agent should be loosely coupled.

The Airflow Developer Agent must not depend exclusively on the Intake Agent.

A user should still be able to take an intake file from their local machine and upload it directly to the Airflow Developer Agent.

--------------------------------------------------
UI REQUIREMENTS
--------------------------------------------------

Preserve the existing FBPA AI Agent Run Console styling and layout.

Do not unnecessarily change:

- Header
- Sidebar styling
- Existing agent navigation
- Existing BSA Agent
- Existing Infrastructure Build Agent
- Existing Tester Agent
- Existing Production Support Agent

Only add the new Intake Agent and restructure the existing Airflow flow.

The sidebar should clearly show:

Home

FBPA Developer Code Agent

Intake Agent

BSA Agent

Infrastructure Build Agent

Tester Agent

Production Support Agent

--------------------------------------------------
NAVIGATION BEHAVIOR
--------------------------------------------------

When clicking:

FBPA Developer Code Agent

show the existing service selection:

- Airflow
- Glue
- Lambda
- etc.

When clicking:

Intake Agent

show the intake-service selection:

- Airflow
- Glue
- Lambda
- etc.

For now, implement the complete Intake Agent functionality for AIRFLOW.

For other services, create the architecture/hooks needed for future implementation but do not invent functionality that does not already exist.

--------------------------------------------------
VALIDATION / USER EXPERIENCE
--------------------------------------------------

Do NOT repeatedly ask the user to confirm or validate information that has already been entered.

Do NOT introduce unnecessary confirmation screens.

Do NOT ask the user to manually copy/paste the generated prompt into a terminal.

The application should handle the flow internally.

If required information is missing, show the specific missing field/error.

Otherwise proceed automatically.

--------------------------------------------------
IMPORTANT ARCHITECTURAL REQUIREMENT
--------------------------------------------------

Keep the responsibilities clearly separated:

INTAKE AGENT:
- Collect requirements
- Validate intake information
- Generate intake form
- Upload intake form to SharePoint
- Make structured intake available for downstream processing

DEVELOPER CODE AGENT:
- Accept intake
- Parse intake
- Generate/modify code
- Apply existing service-specific code-generation logic

Do not mix these responsibilities.

--------------------------------------------------
IMPLEMENTATION EXPECTATION
--------------------------------------------------

Before modifying files:

1. Inspect the existing project structure.
2. Identify the current sidebar/navigation implementation.
3. Identify the current FBPA Developer Code Agent implementation.
4. Identify the existing Airflow form fields.
5. Identify the existing intake-form generation logic.
6. Identify the existing SharePoint upload logic.
7. Identify the existing Airflow code-generation logic.
8. Identify how state/session data is currently managed.

Then make the minimum required refactoring.

Do not create duplicate implementations when existing functionality can be extracted and reused.

After implementation, verify that:

1. Existing agents still open correctly.
2. FBPA Developer Code Agent still works.
3. Airflow is still available under Developer Code Agent.
4. Intake Agent appears as a separate top-level sidebar option.
5. Intake Agent → Airflow displays the required intake fields.
6. Intake Agent generates and uploads the intake form to SharePoint.
7. Developer Code Agent → Airflow allows intake upload.
8. A locally uploaded intake can be parsed.
9. The Airflow agent can consume the intake and generate the DAG.
10. The old "Generate Intake Form: Yes/No" option is removed from the Developer Code Agent.
11. No existing Glue/Lambda/other agent functionality is broken.

Do not stop after describing the changes.
Implement the changes in the existing project.